{
  "buildCommand": "npm install && cd app && npm install && npm run build",
  "installCommand": "npm install && cd app && npm install",
  "outputDirectory": "app/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/app", "destination": "/app/index.html" },
    { "source": "/app/(.*)", "destination": "/app/index.html" }
  ]
}
